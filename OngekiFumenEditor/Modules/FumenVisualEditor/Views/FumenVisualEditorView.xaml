<UserControl x:Class="OngekiFumenEditor.Modules.FumenVisualEditor.Views.FumenVisualEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:cal="http://caliburnmicro.com"
             xmlns:converters="clr-namespace:OngekiFumenEditor.Modules.FumenVisualEditor.Converters"
             mc:Ignorable="d" 
             cal:Message.Attach="[Event KeyDown] = [Action OnKeyDown($executionContext)]"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <converters:UnitCloseSizeConverter x:Key="UnitCloseSizeConverter"></converters:UnitCloseSizeConverter>
        <converters:IsStringEmptyConverter x:Key="IsStringEmptyConverter"></converters:IsStringEmptyConverter>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="Auto"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <Canvas Visibility="Visible" AllowDrop="True" Background="Black" DragEnter="Grid_DragEnter" Drop="Grid_Drop" Name="VisualDisplayer">
            <Canvas.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Selected Object...">
                        <MenuItem Header="Delete" cal:Message.Attach="DeleteSelectedObjects()"></MenuItem>
                        <MenuItem Header="Copy"  cal:Message.Attach="CopySelectedObjects()"></MenuItem>
                        <MenuItem Header="Paste"  cal:Message.Attach="PasteCopiesObjects()"></MenuItem>
                    </MenuItem>
                </ContextMenu>
            </Canvas.ContextMenu>
            <ItemsControl ItemsSource="{Binding XGridUnitLineLocations}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <Grid></Grid>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Line X1="{Binding X}" Y1="0" X2="{Binding X}" Y2="{Binding ElementName=VisualDisplayer,Path=ActualHeight}">
                                <Line.Style>
                                    <Style TargetType="Line">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsCenterLine}" Value="True">
                                                <Setter Property="Stroke" Value="#90FFFFFF"></Setter>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding IsCenterLine}" Value="False">
                                                <Setter Property="Stroke" Value="#40FFFFFF"></Setter>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Line.Style>
                            </Line>
                            <TextBlock Opacity="0.35" Foreground="White" FontSize="10" Text="{Binding Unit}">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsCenterLine}" Value="True">
                                                <Setter Property="Visibility" Value="Hidden"></Setter>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                                <TextBlock.RenderTransformOrigin>
                                    <Point X="1"></Point>
                                </TextBlock.RenderTransformOrigin>
                                <TextBlock.RenderTransform>
                                    <TranslateTransform X="{Binding X}"></TranslateTransform>
                                </TextBlock.RenderTransform>
                            </TextBlock>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Canvas>
        <Grid Grid.Column="1" Background="White">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="10,5,10,5">
                    <CheckBox Margin="0,2,0,2" IsChecked="{Binding IsPreventTimelineAutoClose}" Content="IsPreventTimelineAutoClose"></CheckBox>
                    <CheckBox Margin="0,2,0,2" IsChecked="{Binding IsPreventXAutoClose}" Content="IsPreventXAutoClose"></CheckBox>
                    <StackPanel Margin="0,2,0,2" Orientation="Horizontal">
                        <TextBlock VerticalAlignment="Center" Margin="0,0,10,0">UnitCloseSize</TextBlock>
                        <ComboBox Width="80" SelectedIndex="4" SelectedValue="{Binding UnitCloseSize,Mode=OneWayToSource,Converter={StaticResource UnitCloseSizeConverter}}">
                            <ComboBoxItem Content="1"/>
                            <ComboBoxItem Content="1.5"/>
                            <ComboBoxItem Content="2"/>
                            <ComboBoxItem Content="3"/>
                            <ComboBoxItem Content="4"/>
                            <ComboBoxItem Content="4.5"/>
                            <ComboBoxItem Content="6"/>
                            <ComboBoxItem Content="8"/>
                            <ComboBoxItem Content="9"/>
                            <ComboBoxItem Content="12"/>
                        </ComboBox>
                    </StackPanel>
                </StackPanel>
            </ScrollViewer>
        </Grid>

        <Grid d:Visibility="Hidden" Grid.ColumnSpan="2">
            <Grid.Style>
                <Style TargetType="Grid">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding ErrorMessage,Mode=OneWay,Converter={StaticResource IsStringEmptyConverter}}" Value="True">
                            <Setter Property="Visibility" Value="Hidden"></Setter>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding ErrorMessage,Mode=OneWay,Converter={StaticResource IsStringEmptyConverter}}" Value="False">
                            <Setter Property="Visibility" Value="Visible"></Setter>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Grid.Style>
            <Border Background="IndianRed" Opacity="0.75">
            </Border>
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" >
                <TextBlock FontSize="30" Foreground="Wheat">Error:</TextBlock>
                <TextBlock FontSize="20" Foreground="White" Text="{Binding ErrorMessage,Mode=OneWay}"></TextBlock>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
